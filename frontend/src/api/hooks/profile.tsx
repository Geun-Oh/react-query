export const useQueryProfile = () => {
  const baseURL = 'http://localhost:3000';

  // QueryClient를 가져와주세요!

  const GET = ; // 프로필 조회

  const POST = ; // 프로필 추가

  const PUT = ; // 프로필 수정

  const DELETE = ; // 프로필 삭제

  return {
    GET,
    POST,
    PUT,
    DELETE,
  };
};
